.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
data/
qdrant-server/
.env
tasks/
# Runtime per-client vault content — regenerated by `make slice`.
# vault/shared/ is committed (system docs); per-client content is not.
vault/clients/*
# Curation audit traces are runtime artifacts, not source.
vault/shared/_curation/
